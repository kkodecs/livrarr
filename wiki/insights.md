# Active Insights

Top learnings a fresh CC session needs to know. For deeper coverage see linked wiki pages.

> **This wiki is new (April 2026).** It was bulk-ingested from build artifacts and may contain inaccuracies — especially where specs evolved faster than the ingest could track. If you find something wrong, **fix it**. Update the wiki page, update this file if affected, and append to wiki/log.md. The wiki only gets better if you correct it when you spot errors. Code is always authoritative over wiki content.

## Architecture

1. **17-crate workspace.** All deps point toward `livrarr-domain`. `livrarr-server` is the composition root. `livrarr-handlers` owns all route handlers behind a compile wall. See [overview](architecture/overview.md). Core application crates (14): domain, http, db, matching, external-data, identity, enrichment, materialize, metadata, download, library, tagwrite, handlers, server. Plus: `livrarr-jobs` (JobService trait — compile-wall-safe job triggering from handlers without depending on livrarr-server), `livrarr-cli` (stub), `livrarr-behavioral` (test harness).
2. **BIG7 entities:** Author, Series, Work, Release, Grab, LibraryItem, List. See [big7](domain/big7.md).
3. **Work-first, not author-first.** The Work is the primary entity everywhere.
4. **One app, both formats.** Ebooks and audiobooks. Per-media-type monitoring (`monitor_ebook`, `monitor_audiobook`).
5. **SQLite WAL mode.** Every connection: `journal_mode=WAL`, `foreign_keys=ON`, `busy_timeout=5000`.
6. **Collections = root folders.** 1:1 mapping. A collection IS a root folder with a name and shared toggle.

## Coding Patterns

7. **trait + impl + stub.** Trait in domain, impl in crate, stub in behavioral. See [async-service](patterns/async-service.md).
8. **`trait_variant::make(Send)`** — not `async-trait`. Non-dyn-compatible — use generics/enum dispatch exclusively.
9. **No SQL outside livrarr-db.** No business logic in handlers. Handlers: validate → call trait → map result.
9b. **Compile wall.** `livrarr-handlers` must NOT depend on `livrarr-db`, `livrarr-metadata`, `livrarr-tagwrite`, or `livrarr-download`. Handlers are generic over `S: AppContext`. Verify with `cargo tree -p livrarr-handlers`.
9c. **Arc<ServiceImpl> pattern.** All service fields in AppState are `Arc<T>`. AppContext type is the inner `T`. Accessor returns `&self.field` — deref coercion handles `&Arc<T>` → `&T`. Service impls don't need Clone.
9d. **Circular dep: OnceLock<Box<AppState>>.** Services that call functions taking `&AppState` (ImportService, ReadarrImportWorkflow) can't hold `AppState` directly — infinite-size type. Use `OnceLock<Box<AppState>>`: Box is pointer-sized (breaks the compile-time layout cycle), OnceLock allows post-construction init. Call `service.init(state.clone())` after AppState construction. `Arc<OnceLock<...>>` and `OnceLock<AppState>` (without Box) both fail — the compiler still needs AppState's size. **Prefer eliminating OnceLock via explicit constructor injection** (pass `Arc<ServiceImpl>` at construction time) whenever a refactor makes the deps explicit. OnceLock is the escape hatch when full refactoring is impractical. **As of the architecture-excellent sprint, zero OnceLocks remain in the codebase** — `LiveImportService` and `LiveReadarrImportWorkflow` were both refactored to explicit fields. Do not reintroduce.
9e. **Trait signature type safety.** Service traits in `livrarr-domain/src/services.rs` must not reference types from walled-off crates — `TaggableItem` (livrarr-tagwrite), `Create*DbRequest` (livrarr-db), `TagMetadata` (livrarr-tagwrite) are banned from signatures. Use domain equivalents: `LibraryItem` for `TaggableItem`, domain request structs for DB request types. Note: `WorkId`/`UserId` are safe — they're defined in livrarr-domain, not livrarr-db. Server impls convert at the boundary.
9f. **Accessor newtype wrappers for orphan rule.** When handlers need server-owned infrastructure (logs, caches, atomics), define a minimal accessor trait in `livrarr-handlers/src/accessors.rs` and a newtype wrapper in server's `state.rs` that delegates to the real type. Required because: compile wall blocks putting the trait in server, orphan rule blocks impl'ing a handler trait on a server type, and `trait_variant::make(Send)` blocks `dyn Trait` (insight 8). Wire the wrapper as the AppContext associated type.
9h. **Handlers bind narrow `Has*` capability traits, not full `AppContext`.** Each handler function uses bounds like `S: HasWorkService + HasAuthorService` — only the capabilities it actually calls. `AppContext` is a blanket supertrait union kept for route composition (where the router needs all capabilities). All capability traits are named `Has*` and live in `livrarr-handlers/src/context.rs`. Adding a new service = add a `Has*` trait in context.rs, impl it on `AppState` in state.rs, and import the narrow bound in the handler.
9i. **Credential traits are isolated from settings traits.** `DownloadClientCredentialService` (provides `get_with_credentials`) is a separate trait from `DownloadClientSettingsService` (CRUD without secrets). Same split for `IndexerCredentialService` / `IndexerSettingsService`. Jobs/handlers that need plaintext credentials bind the credential trait; read-only or config-only handlers bind only the settings trait. This is compile-time RBAC groundwork — when user-tier handling arrives, credential services won't be injected into user-tier handlers.
9g. **Handler-level spawning for background work.** Services receive `&self` and can't clone `AppContext` or move it into `tokio::spawn`. Handlers own `State<S>` and can `state.clone()` + `tokio::spawn`. Use this for fire-and-forget side effects (bibliography refresh, author monitor) and long-running background jobs (bulk refresh). OnceLock<Box<AppState>> (9d) is the escape hatch for when a service *must* access full state; handler spawning is the default. **`AuthorMonitorWorkflow::trigger_monitor()` no longer exists** — the no-op stub and its trait method were deleted in the Phase-1 dead-code cleanup (commit `af709f01`, audit M-006); the trait now has only `run_monitor` (`livrarr-domain/src/services/monitor.rs:31-37`, verified 2026-07-10). The on-demand trigger from handlers uses `tokio::spawn + run_monitor` directly (this pattern).
10. **All blocking I/O in `spawn_blocking`.** Never block the async executor.
11. **`chrono` for datetime.** Never `time` crate. Project-wide.

## Metadata

12. **Never use OpenLibrary for foreign language metadata enrichment.** OL IS used for foreign search/discovery (with language filter). Google Books provides the actual foreign-language metadata. See [metadata-sources](domain/metadata-sources.md).
13. **No LLM chooses a match anywhere (Phase 5, 2026-07-03).** Goodreads matching is fully deterministic (junk-edition filter + the shared `pick_best_candidate` authority picker + explicit abstain) and participates in identity/enrichment for every install — the `llm_configured` gate and the ResolverConfig field are DELETED. Hardcover's Tier-2 LLM pick (`llm_disambiguate`) is DELETED: an exact-match miss rides the same shared `pick_best_candidate` as every provider and abstains below the bar (matching-conformance 2026-07-14: the loose 0.75-jaccard accept-bar is gone — the picker is the identity authority's, author bar `author_verdict`; see insight 59). The only LLM on any provider path is `llm_extract_payload` (GR HTML-parse repair) — repair, not selection; a repaired payload carries zero extra trust (no confidence/provenance field exists to upweight it). An earlier version of this insight said "Goodreads requires LLM" — wrong since Phase 5.
14. **LLM privacy boundary:** public metadata OK. Filenames, paths, checksums, prefs, keys, IDs — never.
15. **Identity locked at add-time.** The add flow settles identity deterministically (`settle_identity` → quorum → anchors; user selection final). An earlier version of this insight said an LLM validator confirms the match — wrong since Sprint E unwired it (REQ-005, 2026-06-14); the dead `llm_validator` module was deleted 2026-07-13 (quality-waves Wave 1). No LLM confirms or selects matches anywhere (insight 13).
16. **Foreign works skip English-centric providers via `works.language`.** The applicability rule (server `main.rs` ≈908-919) dispatches only GR/Audnexus/GB/Audible for non-`en` works, and the merge chokepoint drops OL/HC payloads for foreign works (`drop_language_incompatible_providers`). `works.metadata_source` is GONE — dropped by migration 061 (Sprint B REQ-018); an earlier version of this insight wrongly claimed it gated foreign refresh.

## Data & State

17. **"Missing" (no file) ≠ "wanted" (monitored).** Don't conflate.
18. **Browser refresh wipes in-memory state.** Restore from persistent source on mount.
19. **Never edit applied migrations.** sqlx checksum validation fails. Always create new migrations. See [migration-pattern](patterns/migration-pattern.md).
20. **INSERT OR REPLACE is banned.** Use `INSERT ... ON CONFLICT (...) DO UPDATE SET ...`.
21. **Per-media-type monitoring.** `monitor_ebook` and `monitor_audiobook` are independent booleans, not a single `monitored`.
22. **Enrichment timeout is 10s per provider** (amended from 3s). Hardcover GraphQL regularly takes 2-5s.

## Process

23. **Build infra first.** Don't start with in-memory fakes unless infra is genuinely complex.
24. **No band-aids.** Fix where data is created wrong, never add downstream workarounds.
25. **Prototype external endpoints before writing parsers.** curl the URL first. See [metadata-sources](domain/metadata-sources.md) for gotchas.
26. **Add duplication check after implementation.** AI will reimplement existing logic.
27. **No build commentary in code comments.** Comments describe what code IS, not how it got there.
28. **SABnzbd `search` parameter searches by name, not nzo_id.** See [usenet-pipeline](architecture/usenet-pipeline.md).
29. **URL-encode all API keys in query strings.** String concatenation without encoding breaks keys containing `&`, `=`, or spaces. Use `urlencoding::encode()`.
30. **All outbound provider HTTP flows through ONE process-global queue** (`livrarr-http/src/outbound_queue.rs`, Phase 3 metadata-remediation, commits 19af4d5..97963cf). Per-`RateBucket`: pacing (GR 1.5s, OL/HC/GB 1s, Audnexus 2s, Audible 150ms, OL-covers 3s pace-only), in-flight cap 2, priority ordering (Interactive > High > Normal > Low; user search/admin-tests Interactive, Add flow High, monitors/convergence/backfill/list-import Low), and a circuit breaker allowlist (`breaker.rs`): `None`/covers are pace-only (R-5/R-6); the six book-provider buckets each carry one breaker; and `Indexer` is breaker-tracked at TWO levels as of #130 (transport per-origin + rate-limit per-indexer — see insight 71; an earlier version of this insight wrongly said `Indexer` was pace-only). Callers WAIT up to a per-priority reserved depth cap; past it, admission rejects with typed `AdmissionError::QueueFull{retry_after}` → `FetchError::QueueFull` → (alpha-hardening fixes, 2026-07-23) all four provider adapters map it to `ProviderFetchError::QueueFull` → `WillRetry{QueueFull}`, which — like `CircuitOpen` — consumes NO retry budget (audit #6: it previously dead-ended as a terminal error in the adapters). The pre-Phase-3 "token bucket rejects → WillRetry{RateLimit}" pattern is GONE with the enrichment queue's TokenBucket. Breaker-open surfaces as `FetchError::CircuitOpen{retry_after}` → clients map to `WillRetry{CircuitOpen}`, which consumes NO retry budget (`record_will_retry_paused`, R-11 — a pause, never a step toward a terminal dead-end). Breaker signals: `do_fetch` reports transport failures it terminalizes; clients report response-derived Success/Failure/TripImmediately (GR anti-bot body → 1h trip; GB 403 quota body → trip until ~Pacific midnight). The enrichment queue keeps ONLY domain duties: applicability and retry budget. The suppression machinery is GONE (pipeline-hygiene, 2026-07-12, PO-approved after cross-family unanimity): `ProviderOutcome::Suppressed`, `OutcomeClass::Suppressed`, `PermanentFailureReason::SuppressionExhausted`, `record_suppressed`, and the `max_suppressed_passes`/`max_suppression_window_secs` config fields are all deleted — it never had a production producer; migration 073 cleared legacy rows and dropped the `suppressed_passes`/`first_suppressed_at` columns. (Unrelated same-word concepts still live: RSS grab suppression, cross-format decline suppression, identity anchor-conflict suppression, and the identity-completion dead-end "suppression" wording in REQ-008 tests.) **Provider-response honesty (2026-07-27, commits 8bd41d37 + 873f8da4):** a completed 2xx is healthy only once the client reads it as the route's promised application response — every decode/envelope failure files exactly one response-derived breaker `Failure` at the leg that discovers it (`hc_post` is the single GraphQL envelope authority incl. editions and discovery's `lookup_hardcover`; Goodreads autocomplete uses a checked top-level parser; `data`+non-empty-`errors` is rejected, partials never consumed). `BreakerSignal::Success` is singular per operation and owned by the client boundary (`AudibleCatalogClient`/`AudnexusClient`::`fetch`/`fetch_by_asin` terminal healthy arms incl. item-404/410 absences so half-open can close; `GoodreadsClient::fetch` gated on resolution `all_legs_succeeded`) — request helpers never report Success. All four scrape/REST clients are generic over a defaulted `HttpFetcher` (production stays concrete) so real client boundaries are hermetically testable; every request-driving test holds the shared breaker guard. Design: `docs/design-provider-response-honesty.md` (as built).
31. **Filter grabs by download_client_id before matching.** The download poller must scope grab matching to the specific client being polled. Without this, grabs from one client can be adopted by another.
32. **NotConfigured is terminal but resettable.** `OutcomeClass::NotConfigured` is in `is_phase2_terminal()` (won't auto-retry), but `reset_not_configured_outcomes()` deletes those rows when config changes. Called from `update_metadata_config`.
33. **Per-provider error enums, not shared ProviderError.** Each provider (Hardcover, etc.) has its own error type mapped to `ProviderOutcome` at the call site. Don't create a generic shared error taxonomy prematurely.
34. **RSS sync tests were masked.** `cargo test` stops at the first failing binary. The author_monitor 429 test was hiding 3 pre-existing RSS sync test failures. Always run `cargo test` with `--no-fail-fast` to see all failures.
35. **Path mapping needs boundary check.** `starts_with` on strings matches `/data/downloads2` against prefix `/data/downloads`. Use `path_starts_with()` which requires `/` separator or exact match.
36. **AtomicBool execution guard + CancellationToken cooperation for background workflows.** Workflows that can be triggered by both a scheduled tick and a user-facing handler (e.g., author monitor) hold an `AtomicBool running`. `swap(true, AcqRel)` returns the prior value — if `true`, return `Err(AlreadyRunning)` immediately. The AtomicBool is **global** (not per-user): `AlreadyRunning` causes the **entire tick** to `return Ok(())` — not skip-and-continue — because the workflow is already executing for some user and attempting others would block on the same lock. All other per-user errors use warn-and-continue (never let one user's failure stop others). User-scoping pattern for scheduled jobs: iterate all users at the job layer, call the workflow per user. **CancellationToken: all sleeps must use `tokio::select!`.** Background workflows receive a `CancellationToken` threaded from scheduler → job tick → per-user workflow. ALL sleeps (inter-item delays, 429 backoffs) must use `tokio::select! { _ = sleep(dur) => {}, _ = cancel.cancelled() => return Ok(partial_report) }` — not just `cancel.is_cancelled()` at iteration boundaries. A bare `tokio::time::sleep()` blocks graceful shutdown for the full duration (a 60s 429 backoff blocks shutdown for a full minute).
37. **SSRF: trusted-infrastructure pattern (alpha4 lesson, do not repeat).** `AppState` carries two HTTP clients: `http_client` (unrestricted) and `http_client_safe` (rejects private/loopback IPs via `SsrfSafeResolver`). The split is **trusted-infrastructure vs runtime-derived URLs**, NOT "hardcoded vs user-supplied". Use `http_client` for **admin-configured infrastructure** that may legitimately live on a private IP — download clients (qBittorrent, SABnzbd, Transmission), indexers (Prowlarr, NZBHydra2, Jackett, direct Torznab), Readarr import endpoints, LLM endpoints. Use `http_client_safe` for **runtime-derived URLs** — cover proxy fetching arbitrary metadata-provider image URLs, anything from a scraper response, anything where the URL came from outside the admin's configuration. Plus there's a `TrustedOrigins` allowlist (built from configured indexers/download-clients at startup) so the grab flow can fetch download URLs from those origins even when the client used is `http_client_safe`. **Reviewer trap:** an audit reviewer (or CC) seeing "user-provided URL" in a handler will instinctively suggest `http_client_safe`. That instinct is wrong here. Admin-configured = trusted = `http_client`. Reverting this distinction breaks every alpha4+ user with private-network infrastructure (the typical Docker/NAS deployment). Alpha3 shipped with that bug; alpha4 was the hotfix. See `wiki/decisions/key-decisions.md` § "SSRF: Trusted Infrastructure Pattern".
38. **Per-crate reference docs in `wiki/crates/`.** `handlers.md` has the full `Has*` trait table and route handler inventory. `domain.md`, `server.md`, `db.md` cover their respective crates. These pages are NOT linked from `wiki/index.md` — navigate to `wiki/crates/` directly.
39. **SettingsService split into 7 narrow traits.** The former 34-method god trait is now: `AppConfigService` (user/tenant preferences: naming, media mgmt, metadata config, email, language validation), `DownloadClientSettingsService` (CRUD, no secrets), `DownloadClientCredentialService` (plaintext credential access), `IndexerSettingsService` (CRUD + Prowlarr config + indexer config, no secrets), `IndexerCredentialService` (plaintext credential access), `RootFolderService` (CRUD), `RemotePathMappingService` (CRUD). Prowlarr config lives in `IndexerSettingsService` — NOT `AppConfigService` — because it is indexer infrastructure (admin), not a user preference. Handlers/jobs bind only the trait(s) they need. **Server impl stays one struct:** `LiveSettingsService<DB>` implements all 7 traits; AppState holds a single `Arc<LiveSettingsService>`. Don't split the impl into 7 structs — that complicates DB wiring unnecessarily. Adding a new settings area: new trait in `livrarr-domain/src/services/`, new `impl` block in `settings_service.rs`, new `Has*` in `context.rs`, new `Has*` impl on `AppState`.
40. **`infra/import_pipeline.rs` is pure utilities — never add orchestration here.** After Phase 3 migration, this file contains only free functions that take all dependencies as explicit parameters — no AppState access, no service trait calls, no DB. Some functions do make network calls (`fetch_qbit_content_path`, `fetch_sabnzbd_storage_path`) via an explicitly-passed `HttpClient`, not through the service layer. All import orchestration (coordinating services, mutating state) lives in `LiveImportService`. When adding new import functionality: pure computation/explicit-I/O → free function here; service coordination → method on `LiveImportService`. The file name "pipeline" is misleading — it's a utility module.
41. **Module-level composite context traits for cohesive handler groups.** Handler modules with many tightly-coupled handlers (`opds.rs`, `manual_import.rs`) define a named composite trait (`OpdsHandlerContext`, `ManualImportHandlerContext`) built directly from `Has*` traits — narrower than `AppContext` but shared across all handlers in the module. Avoids repeating long bound lists per-function and makes the module's capability contract explicit. Pattern: `pub trait XHandlerContext: HasA + HasB + ... + Clone + Send + Sync + 'static {}` with a blanket `impl<T: HasA + HasB + ...> XHandlerContext for T {}`. These traits do NOT extend `AppContext` — they select only the `Has*` traits the module actually uses. Use this pattern when a module has 5+ handlers sharing the same set of services.
42. **Author monitor has three independent controls: `monitored`, `monitor_new_items`, `monitor_since`.** `monitored=true` includes the author in the daily scan. `monitor_new_items=true` auto-adds newly-detected works; `false` creates a notification-only entry (user sees the new work but it's not added to the library). `monitor_since` (optional `DateTime<Utc>`) filters out works whose publish year predates the threshold — only works newer than this date are candidates. These are Author-level fields, distinct from work-level `monitor_ebook`/`monitor_audiobook` (insight 21).
43. **OpenLibrary author works endpoint structure.** The author monitor hits `https://openlibrary.org/authors/{ol_key}/works.json?limit=100`. Response top-level key is `entries` (not `works`, `results`, or `items`). Each entry's `key` field is `/works/OL12345W` — strip `/works/` prefix to get the bare OL work key. `first_publish_date` is unreliable: can be `"2025"`, `"January 1, 2025"`, or other locale-specific formats — parse by finding the first 4-digit numeric token (`.split(!is_ascii_digit()).find(|tok| tok.len() == 4)`). No auth. Rate bucket: `RateBucket::OpenLibrary`. Timeout: 30s.
44. **OL covers: lookup by ISBN/OCLC/LCCN is rate-limited 100/IP/5min; lookup by Cover ID or OLID is unlimited.** Always resolve any ISBN/OCLC/LCCN cover request to a Cover ID exactly once, persist that Cover ID on the work, and fetch by Cover ID forever after. Cover IDs are immutable. The ISBN/etc. cover endpoint is documented as crawling-prohibited (OL's words: "Please, do not crawl our cover API"). See [`wiki/integrations/openlibrary.md`](integrations/openlibrary.md).
45. **OL anti-pattern: hundreds of `/works/{key}.json` round-trips.** OL explicitly names this as a bad-citizen pattern. Use `search.json?q=...&fields=...` for batch retrieval whenever the alternative is N single-record fetches. Bulk operations (>1k records) should not hit the API at all — use the monthly data dumps (TSV+JSON, available via torrent) and the RecentChanges API for freshness deltas. The full OL operational guide is at [`wiki/integrations/openlibrary.md`](integrations/openlibrary.md).
46. **Door→road wiring is untested — trace it at design.** Behavioral tests cover the *pipeline* (`run_unified`/materialize), not the *door→pipeline wiring*. A handler can compile and pass the whole suite while routing **off** the one road — the add-from-search door set `skip_sync_enrichment` + spawned its own enrich/cover route, caught only by a deep audit, forcing the Option-A cutover. At design, trace every entry door/handler into the canonical pipeline; threading a value into a struct (`candidate_id` into `WorkCandidate`) is **not** the same as the door being wired. 2nd consecutive feature where a signal reached one path but not all of them (cf. metadata-modularization cross-layer threading). A new R1/R2 door is not done until its row exists in `test_door_gate.rs`. (metadata-refactor retro, 2026-06-09)
47. **Explicit System Truths pay off — keep the section content-rich.** A spec enumerating ST-001-style environment facts (provider audio capabilities, GB quota, anti-bot) correlated with **0 missing-system-truth review findings** — the class that recurred in prior features (raw-SQL writers, SABnzbd param, OL language filter). `verify.py` requires the System Truths section; the *content* is what kills the class. Enumerate real environment facts, never a pro-forma placeholder. (metadata-refactor retro, 2026-06-09)
48. **The canonical architecture model is live at `docs/canonical-model.yaml`** (authored 2026-06-10; moved from `architecture/` in the 2026-06-29 cleanup). The entity spine (17 concepts — HistoryEvent added by the work-history architecture amendment, 2026-07-18), legal crate seams (full `livrarr-*` names), data_flow + invariants, and the amendments log. Gate rules: a feature IR v1 must carry `domain_entities` (names-only list of the spine concepts it introduces/touches; `[]` is valid) — only that marked set is spine-gated; IR crate-level dependency edges must be legal per `seams` (conceptual feature-internal edges are ungated); `verify.py canonical` reverse-checks every entity name against `pub struct/enum` in source. Any spine change (entity names or seam edges) requires a new `amendments[]` row. Deliberate not-yet-conformed reds stay ≤2 and logged — currently 1: `Release` (code carries `ReleaseSearchResult`; rename = issue #141). The live `library→tagwrite` edge is off-model (intent: save via `materialize` — amendment S1). Conformance backlog is tracked: issue #141 (Release rename), issue #143 (library→materialize cutover). The model↔code audit tool is `python3 ~/Projects/kk-build/audit_canonical.py /mnt/opt/livrarr` (report-only; exit 1 = hard drift; `--full`/`--json` for detail). First-audit baseline (2026-06-10, post-merge ab28699): entity_coverage 0.9375 (Release), seam_conformance 0.9787 (library→tagwrite), surface_sanction_ratio 0.024 (652 unsanctioned pub types, 154 true-noun candidates in livrarr-domain) — both hard-drift hits are exactly the two on-record deltas. **During the first feature through the armed gates, report every point of gate friction verbatim** (unclear error, awkward convention, a check firing on something legitimate) to the kk-build side instead of working around it — never soften a gate locally; the escape is an amendment or a kk-build-side fix.

49. **Speed baseline (2026-06-10, `d1b8768`) + the a6 release gate.** Lookup is parallel (`tokio::join!` in work_service.rs ≈1592, 8s/leg) but the enrichment scatter is **already PARALLEL** — **CORRECTION 2026-06-14:** the original "serial / zero concurrency primitives" claim was WRONG. `dispatch_enrichment` (`livrarr-enrichment/src/provider_queue.rs`) fans every provider into a `JoinSet` with a per-provider `Semaphore` + GCRA `TokenBucket`, present since the 2026-06-04 carve (`808d47a`) — i.e. BEFORE this baseline. So within-work fetchers run concurrently and GR enrichment IS rate-limited (per-provider token bucket, shared `Arc`). The per-work ≈2.2s, if still real, is dominated by LLM/pacing, not serial fetches. What IS serial is **bulk refresh across works** (`refresh_all` serial `for` loop, `handlers/work.rs:631`). The per-work timing needs a re-measure — the baseline doc and the live code disagree. Bulk refresh = serial×serial (144 works ≈ 8min, perfectly linear 3.3s/work). Scan ≈1.3s/row with warm == cold (cache likely not consulted on the scan path). Artifacts: report at `docs/speed-baseline-2026-06-10.md`; harness `scripts/speed-baseline.py` + raw JSONs in `build/reports/` (local, untracked — scripts/ and build/ are gitignored by convention). **PO decisions (2026-06-10):** `v0.1.0-alpha6` ships only when Sprints A–F are ALL complete — alpha 6 the theme and the tag are one thing, no early cut. The scatter parallelization must be the *principled* version (all anchors incl. ASIN resolved in the identity phase first — pairs with #144; pragmatic chained-pair variant rejected). **Ops rule:** bulk refresh on a live library exercises F1 — the 2026-06-10 capture wrote wrong-book merges onto 8/13 foreign works (reverted from the pre-migrate backup; forensic copy `testdata/livrarr.db.f1-damaged-20260610-183104`). Snapshot the DB before any write-heavy run and check the open critical-bug list against the code path first.

51. **Sprint B (metadata-correctness): CODE GATE PASSED 2026-06-11; Test stage in progress — a PO bug-fix wave (#147–#153) landed on top, ALL UNCOMMITTED; workspace 935/0.** (1) `apply_enrichment_merge` writes NO anchor columns and never touches `external_ids` — anchors move exclusively via the identity track (REQ-007; closes the F1 write path); None/empty cover_url, language, title, author_name never clobber populated values (COALESCE + empty-filtered binds). (2) Enrichment dispatch is **anchor-grounded** (REQ-006): the queue derives a per-provider `AnchorQuery` (GB←isbn, GR←gr_key ONLY, HC←isbn∥hc_key, OL←ol_key∥isbn, Audnexus/Audible←asin) and calls `ProviderClient::fetch_by_anchor` — no text-search fallback is reachable from enrichment; an anchorless provider gets a SkippedNoAnchor call record + NotFound outcome and no spawn. `fetch(&Work)` remains only behind lookup/identity surfaces. (3) **HC by-key gap: CLOSED since** — the enrichment dispatch arm now supports it (`provider_client.rs` fetch-by-anchor doc: "Hardcover: HcKey or Isbn13"; verified 2026-07-12 during the U-B2 confer). An earlier version of this insight said no by-hc_key query existed. (4) Title-variant folding (REQ-020) is live in m4 scoring with a sibling-volume guard (variant-key equality scores 1.0 only when known series positions don't conflict); position-less same-series siblings can still collapse — the Test-stage #132 run must scrutinize sibling pairs. (5) Call records live in `provider_call_records` (059), dissents in `work_field_dissents` (060, FK CASCADE — test fixtures must seed users/works). (6) **Merge dissents are live (L4):** a Conflict provider is dissent-isolated (PayloadMismatch rows per offered field) and never blocks the merge; foreign works get per-text-field LanguageIncompatible dissents (REQ-013) with the last-known-good echo preserving current values; the dissent store reflects the LATEST merge — a zero-dissent merge clears stale rows; REQ-007 is now structural (`UpdateWorkEnrichmentDbRequest` carries no anchor fields, `ApplyEnrichmentMergeRequest` no external_id_updates); an all-excluded provider set yields a status-only apply, and all-conflict now resolves to `Failed` (Unenriched-on-conflict sentinel retired). (7) **Identity completion is live (L5):** `complete_anchors` (async_resolver.rs) resolves missing anchors on refresh — suppression-bounded (all-suppressed = zero network), monotonic via merge_missing_anchors, no writes on non-Resolved outcomes; the resolver's audiobook axis now fires for ASIN-less title+author seeds (REQ-010). (8) **Cover dims (L6):** materialize decodes width/height from saved cover bytes into `MaterializeOutcome.saved_cover`/`saved_audiobook_cover`; decode failure never fails the save. _Amended by N2 (2026-07-04):_ both slots now persist dims — the gate stamps measured dims at every commit, phase-1 measures what it wrote, and `update_audiobook_cover_dimensions` closed the audiobook gap. (9) **ChapterExtractor seam (L7):** livrarr-library carries no tagwrite edge; the server's `chapter_extractor::ChapterExtractorImpl` is the delegate, tests use `stubs::TagwriteChapterExtractor`. (10) **Orchestration (L8):** refresh runs suppression-aware `complete_anchors` BEFORE the scatter (unresolved completion records a NotFound terminal that parks the provider; resume = explicit retry reset or identity edit); ALL SIX add doors funnel through `ensure_identity_and_enrichment` (anchor-less → resolver.resolve(Interactive) → monotonic anchor merge → Confirmed/Provisional badge → enrichment when identity permits and the work needs it); `skip_sync_enrichment` is DELETED; Refresh-All list filtering gained the language facet; GB lookup no longer stamps query language. (11) **Server wiring (L9):** call records flow through `server/call_sink.rs` (bounded mpsc 4096, 64/2s batches, drop-counted, drained on shutdown) wired at all three client construction blocks + queue + enrichment service; `call_record_retention` job evicts at 30d/100k every 6h; `init_tracing` returns `LogSurfaceStatus` (failures captured + file layer skipped, never `.ok()`-swallowed) and `/system/status` reports the real dated log file; `/system/provider-stats` serves record-fed 24h aggregates; `health_summary` + metadata-config provider statuses are stats-derived ("in error" = last error newer than last success); `ProviderHealthState` and `AppState.refresh_in_progress` are GONE (single sources of truth: stats service, WorkService guard); `refresh_all` accepts language/monitored/enrichment_status/media_type query params (UI doesn't send them yet). See `~/Projects/kk-build/build/state/session-log-metadata-correctness.md` for the layer checkpoints.

52. **Test-stage fix wave (2026-06-11 night) — durable mechanics a future session must know.** (1) **Quorum agreement semantics** (`english_identity_resolver::agree`): a payload with no author ABSTAINS (agreement then needs exact normalized-title equality); a shared edition bridge (isbn/asin) corroborates ONLY absent contradicting titles — same ISBN + flatly disagreeing titles is an ISBN collision and must Conflict (AC-020, the wrong-book guard); a key-only text-less payload is unclusterable BY DESIGN (an unverifiable key never rides in). Provider payloads must therefore carry title/author: HC author + OL title were missing (now fixed) — when a provider's key never lands, check its payload shape first. (2) **GR key resolution is AUTOCOMPLETE-FIRST** (PO decision after the LLM-recalled key 10266 proved wrong live): established key → isbn: autocomplete → title/author autocomplete + LLM-disambiguate-among-real-hits → LLM-recall last (quarantined: on parse failure the key returns bare unless one autocomplete round-trip confirms it — `fallback_key_payload`; the work's own title is NEVER stamped, a hallucinated key must not self-verify). (3) **Identity fan-out forensics exist**: debug lines `identity fan-out responder` (per-provider payload shape) + `identity quorum ...` — the identity road emits no call records (per-client sinks instrument fetch_by_anchor only); these log lines are the only visibility. A leg that hangs past the 10s budget is silently dropped (GR throttles this way). (4) **Cover trust lock (#153)**: add-time covers stamp `validated` (piggyback) and `CoverTrust` forbids Validated→Unvalidated replacement. _Amended by N2 (2026-07-04):_ the upgrade half is now LIVE — the cover write gate (insight 63) runs the measured comparator on every enrichment pass, so a below-floor validated cover IS replaceable by a validated-better candidate; only User covers are absolute. (5) **rbook 0.4 silently dropped all OPF metadata after an empty self-closing element** (`<dc:format/>` — old OEB conversions), losing embedded ISBNs; 0.7 fixed it (verified on the real file); the scan matcher now also key-matches on embedded isbn/asin (`ProviderKeys` at the manual-import scan).

50. **Sprint B evidence round (2026-06-10) corrected four standing claims** — full evidence with file:line + SQL in `spec-metadata-correctness.md` §0b. (1) **F1's root cause is wrong-BOOK adoption**, not a missing language guard: anchor-less works hit the providers' title/author fuzzy-search fallback on refresh (GR/GB); the HC/OL foreign drop is centralized at the `merge()` chokepoint and HELD during the incident (0 damaged fields from HC/OL). (2) **Anchor state is TRIPLE-stored** — inline works columns, `work_identity_anchors` ledger, `external_ids` table — and the enrichment merge writes two of them (raw binds `sqlite_work.rs` ≈912-924, only `asin` COALESCE'd; `UpsertExternalIdRequest` emission `livrarr-enrichment/src/lib.rs` ≈952-980; incident wrote 3 wrong ASINs into `external_ids`). (3) **File logging is ALIVE** (daily rolling files since 2026-04-20); the "dead since April" artifact is the status page pointing at the frozen pre-rotation `livrarr.txt`, and log-dir creation errors are swallowed with `.ok()` (#102's vector). (4) **There is NO 24h enrichment cache** — only a 5-min consume-once transport cache (add path) and a 15-min UI lookup cache; the manual-import scan path consults neither (hence warm==cold in the baseline). Also confirmed: `lookup_goodreads` no longer stamps query language (#11 fixed on main); cover re-resolve on refresh already works and `CoverTrust::User` survives it; `works.metadata_source` has zero readers (see insight 16).

53. **Sprint D (seeds & doors): one seed builder + a backend monitor-language guarantee (committed `5a8e3b9`, 2026-06-13).** (1) **Single seed-construction point** — `livrarr-domain/src/seed.rs`: all 7 work-creation doors build their `WorkCandidate` through per-door `seed_*` constructors; `SeedLanguage::resolve` is the ONE place the `"en"` default lives, and it stamps the resolved language into BOTH `WorkSeedFields.language` AND the identity harvest (`CapturedIdentity`/`RawHarvest`) so they can't disagree (the list door used to). Door-local `WorkSeedFields {...}` literals are now illegal outside `seed.rs` + tests. (2) **`monitor_language` is per-author and per-series** (migration 063, `authors.monitor_language` + `series.monitor_language`, both nullable). Governs the language stamped on works the monitor auto-creates — NOT an edition picker (selecting "de" labels what the monitor adds; it does not fetch German editions — OL biblio/GR roster feeds carry no per-entry language). (3) **The "monitored ⇒ never NULL language" invariant is enforced STRUCTURALLY at the DB write chokepoints, not per UI surface** — `update_author` (guard: monitored && chosen-null → default, fires for unset AND explicit `Some(None)` clear) and `update_series_flags`/`upsert_series` (post-write `ensure_monitored_series_language` heal). So no UI door — current or future — can enable monitoring with a silently-wrong language; the bare list/detail toggles deliberately send no language and rely on this. The smart default = `seed::dominant_language(entity's works)` (unique-max, tie/empty → None) else `"en"` — the ONE shared rule, mirrored by `series_query_service.suggested_language` and the frontend pre-fills. `create_author` inserts unmonitored (DB default 0), so the three guarded writes are the complete enable surface. (4) **#53 author-monitor junk screen** (`author_monitor_workflow.rs::screen_entry`, deterministic, no network/LLM): rejects multi-author anthologies (≥6 credited authors — `OlWorkEntry` now deserializes the `authors` array), bundle/omnibus/box-set/"Books N–M" titles, self-titled bundles (strip author name + bundle words + stopwords + digits → empty = junk; "Persuasion by Jane Austen" must PASS), newline/collapsed-space malformed titles (screened, never repaired), study-guide keywords (high-precision only — "Notes on"/"Analysis of" EXCLUDED, they match real titles), and title-less entries (no more works named "Unknown"). Screened count in `MonitorReport.entries_screened`. (5) **F8: the `llm_scraper` road is DELETED** — `MetadataProvider` trait, `LlmScraperProvider`, configs, the cfg(test) `OpenLibraryProvider`, and the orphaned `ProviderSearchResult`/`ProviderAuthorResult` are gone (with the banned GR `/search` URL template); `ProviderWorkDetail` stays (used by `HardcoverCandidate`). Future LLM discovery (#31) builds on LLM web-search APIs, not HTML scraping.

54. **Series-monitor + Readarr seed `identity-pending` BY DESIGN (M9) — the bug is the missing auto-convergence, a binding-principle regression (as-of 2026-06-14).** M9 (`wiki/domain/metadata-principles.md`) explicitly permits batch/monitor paths (list import, Readarr import, series/author monitors) to create `identity-pending` works that "converge to full identity via the shared async resolver (REQ-022)… never silent limbo, never an indefinite retry loop." So seeding Pending is NOT an oversight. The defect: the recurring convergence (`enrichment_retry_tick`) was REMOVED; `retry_all_incomplete` is user-triggered/single-pass/no-loop (`services/work.rs:310`, POST-route only); the add-time leg only resolves **anchorless** works and won't fuzzy-adopt (`work_service.rs:2666,2720`); `bulk_resolver::resolve_bulk` (the `LatencyTier::Bulk` batch resolver) has **zero production callers**; no `jobs/` task re-resolves Pending. So series-monitor/Readarr Pending works sit in the "silent limbo" M9 forbids until a user manually triggers retry → divergent end states → REQ-022 violation. The five-phase pipeline (identify→seed→create→enrich→materialize) is gated: phase-4 enrichment is blocked behind phase-1 identity (`work_service.rs:2737`), so skipping identify also silently skips metadata. Full map + door matrix: [`architecture/work-creation-pipeline.md`](architecture/work-creation-pipeline.md). (Sprint E prereq #144 remainder; dead-background-convergence PO decision **MADE 2026-06-18**: restore automatic convergence by wiring the existing primitives — `converge_identity_pending` + the due-for-retry query — through the unified pipeline as a feature (Part 1, with interim backoff/no-loop guardrails); throttle/quota governance is the fast-follow (Part 2). Prep notes: `livrarr-convergence-spec-notes.md`.)

55. **Refresh's long pole is the enrichment SCATTER, not the identity re-chase — the gate shipped, the win was modest (analysis 2026-06-14; shipped + re-measured 2026-06-16, sprint-e).** _An earlier version of this insight wrongly called the per-refresh identity re-chase THE refresh cost and projected that gating it would take missing-anchor works ~4s→~0.5s; the shipped gate refuted that._ The enrichment provider scatter is already parallel (`livrarr-enrichment/src/provider_queue.rs:524` JoinSet, since `808d47a` 2026-06-04) + GR-paced (shared per-provider GCRA TokenBucket); the LLM validator was removed from enrichment (REQ-005, `lib.rs:735`). So "parallelize the scatter" (the 2026-06-10 baseline's premise) is moot — and that baseline's "serial scatter / zero concurrency" claim is WRONG (it predates nothing; the JoinSet was live 6 days before the capture). Refresh is **synchronous** (POST blocks till done). `refresh()` USED TO wipe suppression (`reset_for_manual_refresh`) and call `complete_anchors` (`work_service.rs` ~1305–1378) **unconditionally**, re-attempting the unresolvable missing anchor every refresh — even on Confirmed works — with the chased gr_key not even used by enrichment (`skipped_no_anchor`). **Sprint-e gated that re-chase for Confirmed works (shipped `fb77b40`).** MEASURED before→after (before = sprint-e handoff; after = `build/reports/speed-baseline-2026-06-16-refresh.json`): Dune/#7 (missing gr_key) **6.4s→4.3s**; Jade/#2 **2.3s→1.9s**; all-anchor #1 **~0.6s** (never had identity activity). The gate trimmed only **~2s of ~6s** — a Confirmed missing-anchor work is STILL ~4.3s, dominated by the **enrichment scatter's LLM/pacing/provider latency** (already concurrent across providers — so NOT a "parallelize the providers" problem). **Next speed work:** the a6 *principled* anchor-first identity phase (insight 49, #144) for per-work cost, and parallelizing the serial `refresh_all` loop (`handlers/work.rs:631`) for library-wide refresh — NOT more re-chase gating. The 24h `metadata_cache` (migration 056) is DEAD/unwired (secondary lever). F1 is structurally closed on main (bulk refresh safe; snapshot first). Handoff: `~/Projects/kk-build/build/state/handoff-sprint-e-refresh-gate.md`.

56. **`MergeEngine::merge` is the payload-policy chokepoint — add policies THERE, never at one call site.** Both entry paths (the network CAS loop in `enrich_work` and candidate-reuse via `merge_from_cached`) funnel through `merge()`, which applies in order: `drop_language_incompatible_providers` (#133/REQ-027), `apply_gr_cover_gate` (REQ-017: English work + OL key → a Goodreads cover whose title fails the 0.6 Jaccard vs the work title is stripped, `cover_url` + `gr_key` both nulled; the LLM flag is hardcoded `false` — `AskLlm` == strip per REQ-005, so the flag never changed behavior), then `merge_impl`, whose `extract_provider_field` normalizes offered values (`non_blank` for strings, `non_empty_vec` for the two list fields Genres/Narrator — an empty list is no offer: it neither wins the priority walk nor erases stored values through the un-COALESCEd `genres = ?` bind; M-013). History: the cover gate lived network-path-only for months (M-012) because it predated this rule — same divergence shape #133 had. `EnrichmentServiceImpl::new`'s `_llm_configured` param is compat-only (the field is gone). (Phase 4 metadata-remediation, 2026-07-02)

57. **`ConvergeOutcome::Completed` ⟺ the work is not re-selectable.** `converge_outcome` (pure fn in `convergence_service.rs`, exhaustively unit-tested) returns `Completed` only for Confirmed/Provisional identity + Enriched/Thin enrichment + zero chaseable anchors — recomputed AFTER Step 3's dead-end bumps. A settled work still holding a chaseable anchor is `StillIncomplete` (one-cadence backoff; bounded by the dead-end threshold, then it reaches an honest Completed). The job's error arm also sets a one-cadence backoff (no more every-tick re-select of a failing work). The convergence job is enabled by default as of step1-code Unit 1 (2026-07-04; `[convergence] enabled = false` opts out) and its provider calls ride the outbound queue at Low priority. (M-017 tight fix; broader chase policy = id-completeness feature.)

59. **ONE matching authority (Phase 5, 2026-07-03): `livrarr-domain/src/identity_matching.rs`.** All "same book?" decisions route through its verdicts: `parse_title` (main/subtitle/series-markers/junk; colon + spaced-dash split), `title_verdict` (Same = exact cleaned-main equality; Grey ≥ 0.75; VetoVolume on conflicting volumes; one-sided/disagreeing true subtitles demote), `author_verdict` (full-name match order-normalized; Abstain when authorless; any-shared-token is dead), `language_verdict` (declared mismatch = Veto; silent + work ≠ install default = Grey), `id_verdict` (work-key equality wins; same-provider contradiction vetoes and OUTRANKS edition-ID agreement — including in the add-time absorb cascade; edition-ID inequality is no evidence). Colon-truncation is dead at all three historical sites. Auto-same anywhere = Same + Agree/Abstain + no veto; grey NEVER absorbs at dedup/absorb seats (visible duplicate + the merge-two-works action instead) — the ONE ratified exception is AC-004 anchor-trust at the two settle-road seats (insight 72): Grey{OneSidedSubtitle} + agreeing independent hard ID + author Agree. Stored DB keys = `identity_key` (main\u{1}subtitle\u{1}volumes triple + canonical author — siblings keep distinct keys; the `ON CONFLICT DO NOTHING` backstop retains historical semantics); `identity_key_flat` is the SCAN/filename comparison form (space-joined, all-position article drop) — exists because `sanitize_path_component` writes `:` as `_`; never stored. Stored keys recompute via the `_livrarr_meta` `identity_key_generation` marker + idempotent startup backfill (skips ONLY genuine unique-violations; any other DB error fails startup loudly). The resolver reads the install default language via `ResolverConfig.default_language_source` (read at startup from `metadata_config.default_language`). N4 (2026-07-03) closed two gaps: the GR search-hit picker (`gr_best_match`) routes through `parse_title`/`title_verdict` (Same or Grey picks — a subtitled GR record now matches a bare seed; Different/VetoVolume never; decorated hit title parsed so decoration volumes feed the veto), and the quorum's `agree` folds payload `series_position` into the volume VETO only via `title_verdict_with_positions` (conflicting positions never corroborate and never ride the edition bridge; one-sided positions deliberately don't demote). The autocomplete road populates `series_name`/`series_position` from the search-card decoration, and the picked candidate's series evidence travels into fallback/detail payloads (`apply_candidate_text`). Residual: a wrong same-main/same-author volume pick with zero volume evidence anywhere is undetectable deterministically — unchanged from pre-N4. **Matching-conformance (2026-07-14):** the shared provider hit-picker is now `pick_best_candidate` (identity_matching) — the loose whole-string 0.75-jaccard scorers (`score_provider_candidates`, `score_candidates`) are DELETED. All five provider search sites (Audible ASIN-verify + search, OL tier-3 `title_author_search`, HC tier-2, GB text-search) route through it at `accept_grey=false` (Same-only: a Grey title never becomes their provider answer, REQ-008 — their picks can reach a background field-merge via the anchor-only D-005 reuse cache); `gr_best_match` folds onto it at `accept_grey=true` (grey stays returnable for the `verify_gr_payload`/AC-004 hatch, gated downstream). Author bar = `author_verdict` at every site (any-shared-token overlap dead here too, D9/AC-008). OL index-back mapping fixed (compacted candidate list → original `docs` index via `kept`, mirroring HC/GR). Documented follow-up: a grey GR payload legitimately returned still field-merges via the anchor-only cache (the R-6 cache-seat gate).

60. **Grey-park review surface (Phase 5 J2).** Parked works (identity_status NeedsReview) persist their candidate sets with REAL computed scores in `work_identity_review_candidates` (migration 068, written at the NeedsConfirmation→NeedsReview transition). `/identity-review` (GET list, POST {id}/resolve, POST {id}/dismiss) + the ReviewPage surface them alongside Identity-Conflicts (that backend's first frontend consumer). `apply_review_candidate` CLAIMS the park as its transaction's first statement (`UPDATE … SET identity_status='pending' WHERE id=? AND identity_status='needs_review'`, rows_affected checked → `NotParked` → 409) then confirms the candidate's anchors + re-derives the badge in the same transaction — settled works and concurrent resolvers cannot double-apply (same TOCTOU claim pattern as `apply_conflict_resolution`). Dismiss reverts to Pending and clears the candidates row under the same claim. RSS language-grey skips emit `NotificationType::RssLanguageSkipped`, deduped per (work, UTC day) via ref_key — completing AC-022's surfacing.

61. **The Phase-5 decision-diff harness carries FROZEN old-side copies by design** (`tests/behavioral/test_p5_matching_diff.rs`): 15 line-for-line pre-Phase-5 duplicates (old string_similarity with both-empty→1.0, old normalize_title_variants colon cut, old find_matching_work cascade, old normalize_for_matching, …) so rewiring units cannot silently change what the instrument's OLD side measures — the sanctioned exception to the no-duplication rule. NEW sides call live code (identity_matching verdicts, identity_key). A fresh run must reproduce `build/reports/phase5-matching-diff-v2.{md,json}` byte-identically (volatile fields stripped); any deviation means the freeze drifted or the library changed — investigate before trusting either side.

62. **Provider parsers must distinguish "unreadable" from "empty" and WARN — never degrade silently; an empty parse is DRIFT, not truth (N1, 2026-07-03).** Three GR parse paths drifted silent-empty in one day (autocomplete rating type, series-page React redesign, cover desync) — reviews cannot catch this class, only live behavior can. Standing discipline: parse list entries INDIVIDUALLY (one rogue entry drops alone, warn-logged); warn on unreadable-body/shape-drift; never persist emptiness over stored data. GR series pages (2026-07 React layout): books ride in `data-react-props` JSON blobs, primaries FIRST with the header's "N primary works" as the roster cutoff — blob membership includes omnibuses/splits/translations (43318 lists 27 entries for 3 primaries), so membership ≠ primary set; positions exist ONLY in title decorations "(Series, #N)" and are trusted only when the decoration names the page's own series (umbrella pages never borrow sub-series numbers); pagination is the `numWorks/currentPageNumber/perPage` counter blob (numWorks = TOTAL works). Roster hardening: stored-empty reads as absent (heals on open), a shortfall vs the declared primary count refuses a partial roster, the monitor never writes an empty fetch, and every roster save pairs with a `work_count` update (ST-007: count IS roster size). Real captured pages: `crates/livrarr-external-data/fixtures/gr-series-{108562,43318}.html`.

63. **Covers have ONE rank, ONE save gate, ONE layout (N2, 2026-07-04).** All cover decisions consume the single rank table in `livrarr-enrichment/src/cover_rank.rs` (ebook EN: GR→HC→GB→Readarr→OL→Audnexus→Audible; ebook foreign: GB first; audiobook: Audible→Audnexus→HC→GR→OL→GB) — the live picker, the size comparator's tiebreak, and import-time host preference all derive from it. Every non-User cover write flows through the cover write gate (`livrarr-metadata/src/cover_write_gate.rs`): download → measure real pixels → comparator (400×600 floor; good-beats-bad; both-good→rank; both-bad→area) → crash-safe commit (candidate bytes + meta sidecar → DB commit → atomic rename → sidecar cleanup), serialized per (user, work, slot) by a keyed mutex that startup recovery also takes. **The generic enrichment merge writes NO cover columns** — `works.cover_*` always describes the file on disk; startup recovery (`cover_write_gate_recovery.rs`) converges any crash state including provenance (only `DbError::NotFound` discards a candidate; transient errors leave it for the next pass). Cover sources are real provider names (host-derived backfill stamped legacy rows; amazon-family = goodreads for ebook, audible for the audiobook slot). ONE layout: `covers/{user_id}/{work_id}{suffix}.jpg`, suffix `_audio` (never `_audiobook`); startup adopts legacy root files; orphan root files are logged, kept, and NEVER served (no root fallback anywhere — serving, tag embedding, and import all read per-user only). Accepted swap bytes ride `prefetched_bytes` into the retag so file art follows the swap even with no other field change. Startup passes run strictly sequenced in one task: layout migration → gate recovery → provenance backfill (`cover_startup.rs`). **AC-4 sovereignty AMENDED (2026-07-16, PO-approved): a User lock is honored only while its cover exists on disk** — final `.jpg` OR the crash-safe protocol's pending candidate meta sidecar (committed-but-unrenamed, recovery's territory); a User row with NOTHING on disk is a damaged slot and IS replaceable (`trust_blocks_candidate`, cover_write_gate.rs). The damage source: fast-add stamped User trust even when the 3s phase-1 download FAILED (cover_url set, 0x0 dims, no file — permanently locked, live-hit on bazaar work 851); `addtime_cover_trust` (work_service.rs) now stamps Unvalidated on a failed user-pick download so the slot heals on the next enrichment pass. Successful user picks/uploads keep absolute protection, including the crash window (Codex-caught P1: the existence check must count pending tmp+meta as protected or a provider candidate bulldozes a just-picked cover whose rename failed).

64. **Discovery/search is its own service (work-service-split, 2026-07-11, commits `2734fd02..0094e805`).** `DiscoveryService` (3 methods: `lookup`, `lookup_filtered`, `eager_match_by_author`) lives in `livrarr-domain/src/services/discovery.rs`; `DiscoveryServiceImpl<C,H,L=StubNoLlm>` in `livrarr-metadata/src/discovery_service.rs` owns the 4 provider lookups, the LLM tail-filter, the 15-min lookup cache, and the resolver fast-path, implemented as free fns over a Copy `DiscoveryCtx` borrow-context. `WorkService` is 17 methods; `WorkServiceImpl<D,E,H>` (7 fields — no llm, no lookup_cache, no merge/tag/http_client) keeps the lifecycle (add/refresh/enrich/CRUD/merge-works) — its resolver field serves ONLY the identity legs (`settle_identity`). Handlers bind `HasDiscoveryService` for search; AppState carries `Arc<LiveDiscoveryService>`; main.rs's list-service and author-monitor WorkServiceImpl instances have no discovery/LLM wiring at all (they never search). `new_with_all` is gone — `WorkServiceImpl::new(db, enrichment, http, data_dir)` is the constructor. A second `HttpFetcherImpl` instance for discovery is safe: all fetchers share the process-global outbound queue (insight 30).

65. **The behavioral suite compiles only what `crates/livrarr-behavioral/Cargo.toml` registers — 30 of 129 files in `tests/behavioral/` are NOT `[[test]]` targets and have never run** (found by Gemini in the work-service-split review, 2026-07-11; scope verified by manifest scan). The orphans are mostly superseded generations (`*_v21`, `test_metadata_redesign_phase*`, `test_verify_*`, `test_cup_convergence`, `test_db_*`, auth suites). Consequences: "the behavioral suite is green" means the 99 registered targets only; editing an unregistered file is never compile-checked (S1/S2b edited five of them blind before this was known). RESOLVED 2026-07-11 (`773afe8f`): 7 registered (incl. first-ever auth coverage), 3 parked on an explicit allowlist, 20 deleted (git-recoverable), and `test_manifest_guard.rs` now FAILS the suite on any present-but-unregistered file — the error class is closed. FULLY CLOSED later the same day (suite-consolidation session): the PARKED list is now EMPTY — test_verify_e2 ported (pins settle_identity persisting all anchors when ol_key is absent), test_cup_convergence and test_metadata_redesign_phase3a deleted (covered by test_id_completeness / drove the dead `AddWorkRequest` overload), and the registered test_verify_g2 deleted (PO decided Thin=settled is correct behavior, so its red gate was wrong-by-design). PO decisions same session: CI stays Docker-build-only (no cargo-test job — decided, don't re-raise); note the suite was ALWAYS tracked+public (the `tests/` gitignore line only blocks new files — `git add -f` for those). Workspace baseline: 1471 passed / 0 failed / 299 ignored / 143 suites. Full disposition record: `docs/orphan-test-triage-2026-07-11.md` §Resolution. **The guard does NOT cover the inverse class** — a file registered in Cargo.toml but never `git add -f`'d is invisible to it and to every local run, yet breaks `cargo test` on a fresh clone: `test_unified_identity_path.rs` + `test_uip_resolver_tied.rs` were registered by `8d66c7a3` but absent from origin until the responsiveness commit (2026-07-12) force-added them. Registering a new `tests/` file and force-adding it are ONE change. **The class includes FIXTURE files:** `tests/behavioral/fixtures/test_cover_100x150.png` (compile-time-read by test_n2_cover_write_gate.rs:712) sat untracked until 2026-07-13 — fresh clones could not compile the suite; any file a test reads at compile time needs the same `git add -f`. **And a whole DIRECTORY instance (found 2026-07-13, 2a session):** `tests/implementation/` (6 `test_impl_*.rs` files) is registered by NOTHING — no `[[test]]` entry in any Cargo.toml — and has never compiled or run; `test_manifest_guard` sweeps `tests/behavioral/` only. Do NOT author pins there; triage pass pending (mirror docs/orphan-test-triage-2026-07-11.md). Ripple: Wave 1's `create_test_notification` KEEP decision cited `test_impl_secondary.rs:377` as a live caller — that caller never compiles, so the helper is back on the dead-code candidate list.

58. **Process-global breaker state wedges parallel tests — mechanism proven, fixed in `livrarr-external-data`, still open in `livrarr-enrichment` (2026-07-26).** `outbound_queue::shared()` is a `OnceLock` singleton (insight 30), so each test binary's per-provider breakers are ONE piece of mutable state shared by every test in it. A test that deliberately trips a breaker and does not reset it leaves that provider Open for the production duration — **3600s for Goodreads** — and every later test in that binary is refused admission before a socket is opened. Resetting at the END of the tripping test is NOT enough: a sibling scheduled inside that window still observes the open breaker. **The symptom is a hang, not a red test** — a test awaiting its own one-shot server waits forever for a connection its client never attempts (`strace`: `socket`/`bind`/`listen`, no `connect`). It stayed hidden because the Goodreads client used to report `BreakerSignal::Success` on any non-anti-bot 2xx, and `record_success` clears failures AND closes an open breaker, so unrelated passing tests were accidentally cleaning up; removing that premature success (correct on its own merits — a bare 200 does not prove a readable page) made the latent defect reliable. **Do not restore it to make tests pass.** Fix pattern, applied in `livrarr-external-data/src/test_support.rs`: ONE breaker mutex per binary, PRIVATE, reachable only via `lock_gr_breaker() -> GrBreakerGuard`, which resets to production defaults on guard entry AND on drop (drop-guard, so a panicking test cannot leak an Open breaker or a one-strike config); every test that emits a breaker signal, reads breaker state, **or merely drives a real fetcher at that bucket** takes it; and any test standing up a one-shot server bounds its wait with `tokio::time::timeout`. Two same-named locks in different modules serialize NOTHING — that mistake was made here first. **Still open:** `provider_queue_tracer_tests::goodreads_tracer_tests::goodreads_through_queue_returns_success_for_direct_gr_key_lookup` in `livrarr-enrichment` (characterized flake since 2026-07-02, `WillRetry{CircuitOpen}` ~1 in 8 full runs) has the identical shape — its sibling anti-bot test at `provider_queue_tracer_tests.rs:320-365` trips the shared breaker and resets only after asserting, neither holds a lock. `test_support` is `#[cfg(test)]`-private to external-data, so enrichment needs its own guard or the guard moves to `livrarr-http` behind `test-helpers`. Until then, on a full-suite failure of ONLY that test, re-run once. Full write-up: `~/Projects/kk-build/wiki/livrarr/test-suite-shared-breaker.md`. **SWEEP COMPLETE in `livrarr-external-data` 2026-07-26:** the guard's real API is `lock_breaker(bucket) -> BreakerGuard` (one process-wide mutex for ALL buckets, not one per bucket — the queue's pacing and in-flight caps are shared too); `lock_gr_breaker()` is the Goodreads convenience wrapper. 81 further request-driving tests across `openlibrary.rs`, `google_books.rs`, `hardcover.rs`, `audnexus.rs`, `goodreads/client.rs` and `provider_client.rs` now take it — the criterion is **drives a provider request function**, not "emits a signal on the branch this test happens to take", because branch-by-branch reasoning is exactly what leaves the next latent one behind. Serializing them costs nothing measurable (the binary is pacing-bound: 221→222 tests, 12.02s, identical across five runs). Two things the sweep unblocked: an absence-of-signal assertion is only writable once EVERY emitter in the binary holds the lock (Audible's "a 404 from the by-ASIN item lookup must not trip" had to be written as a comment until it landed), and it surfaced a missed production site — `audnexus.rs`'s `cached_fetch` served both an ITEM route and a SEARCH route through one 404/410 absence exemption, so a dead Audnexus search route read as "book absent" with the provider still green (fixed with an `Absence` route-kind parameter; red-first proven).

67. **Author identity has ONE adoption gate + a real merge (author-dedup, 2026-07-12).** Every author-creating door dedups through `identity_matching::unambiguous_author_match` (`livrarr-domain/src/identity_matching.rs` — exactly-one-unambiguous-match over the user's authors; three adoption rules: equal-count pairwise initial/word compatibility, symmetric glued-initials explosion "jk"⇄"j k", exact-full-word-prefix surplus; a lone initial never spans unchecked given names; ambiguity/grey → create separately). Wired at the exact-miss arm of `find_or_create_author` (work_service), `AuthorServiceImpl::add` (BOTH arms use monotonic ol_key fill — never overwrite a populated key; OL hands the same person different keys), and Readarr `process_authors` via pure `resolve_batch_author` (batch-local list: newly-created appended, adopted never re-appended, distinct-id exactly-one). `AuthorDb::merge_authors` (sqlite_author.rs) is the ONE author-merge transaction: works repoint + author_name rewrite + merge_generation bump (tag re-sync; `normalized_author` deliberately untouched — generation backfill owns rekeying), series fold (flag OR, language coalesce + monitored⇒"en" backstop, roster repoint-if-survivor-lacks, work-flag propagation mirroring update_series_flags/link_work_to_series) or move, loser caches dropped, author fields monotonic (booleans OR, monitor_since MIN, monitor_language coalesce + dominant_language-else-"en"), loser deleted — step order is load-bearing against series CASCADE + works.series_id SET NULL. Endpoint: `POST /author/{survivor_id}/merge` `{loser_id}` → `AuthorMergeReport`. Deliberate residuals: bare "Robert Heinlein" adopts onto a sole "Robert A. Heinlein" (blocking would recreate dupes for the commonest provider shape; merge is the recovery); no DB unique backstop (initial-compat is non-transitive); byte-identical concurrent-insert race remains theoretical. Design: `design-author-dedup.md` (12 review rounds; [REV] markers are binding). The 3 live clusters (Rowling/Griffin/Heinlein, 6 rows) were merged 2026-07-12; snapshots `livrarr.db.pre-author-dedup-20260711` + `.pre-author-merge-20260712`.

68. **Persistent provider-response cache: ONE table, ONE seam, policy at the seam (U-B1, 2026-07-12).** `provider_response_cache` (migration 072; global, deliberately no user_id) keys successful detail payloads by (provider, anchor_type, anchor) — anchor_type strings `isbn13|gr_key|hc_key|ol_key|asin` derived from the dispatched `AnchorQuery` variant. The ONLY consult/write point is inside `DefaultProviderQueue::dispatch_enrichment` (`livrarr-enrichment/src/provider_queue.rs`), after applicability → anchor derivation → terminal-skip; `ProviderResponseCacheDb` (livrarr-db) is storage-only — TTL comparison, success-only writes, and the `Freshness` knob live at the seam, never in the DB layer and never at call sites (insight 56's chokepoint rule). `EnrichmentContext.freshness` carries the knob: user refresh (`RefreshSurface::Interactive` AND `Bulk`) = `Bypass` (always fetches, rewrites the row); add/re-add, convergence, monitors, list import = `PreferCache` (fresh row ⇒ zero HTTP; payload rides into budget rules/persistence/merge exactly like a fetched Success). Freshness is orthogonal to `RequestPriority` (D-004). Success-only: errors/not-found are NEVER cached (a NotFound provider re-fetches next pass, still governed by the retry/terminal budget). `fetched_at` stamps at write time; eviction is oldest-first (`fetched_at, rowid` tiebreak) down to `max_rows` after write batches. Config `[metadata_cache] ttl_days=7 / max_rows=100_000`, TOML only. A cache hit emits NO provider call record (records instrument real HTTP at the client wrapper — never invoked on a hit), so call-record-based stats/evidence stay truthful; measured live: warm re-add = zero provider HTTP (baseline doc §6). **Test-fixture trap that bit this suite:** `reset_enrichment_for_refresh` only flips `works.enrichment_status` — it does NOT clear `provider_retry_state`, and both Success and NotFound are phase2-terminal, so a second same-work pass terminal-skips before the cache unless `reset_all_retry_states` (or `reset_for_manual_refresh`) is also called.

69. **HTTP clients are consolidated shared instances; bulk refresh is bounded-concurrent (U-B4/U-B5, 2026-07-12).** main.rs builds exactly FOUR shared HTTP surfaces: `http_client` (unrestricted, 30s+UA), `http_client_safe` (SSRF-safe, 30s+UA), `http_fetcher` (the ONE `HttpFetcherImpl` — every service construction clones it), and `llm_http_client` (unrestricted trust class — LLM endpoints are admin-configured infra per insight 37 — 60s timeout + UA; replaced three bare no-timeout/no-UA builders that could hang unboundedly). New wiring must CLONE these, never construct fresh instances (reqwest clients share their pool on clone; all fetchers already share the process-global outbound queue, so consolidation changed no provider-facing behavior — measured: `docs/connection-reuse-report-2026-07-12.md`, 24 requests → 1 connection per provider host, verdict = keepalive tuning permanently skipped). `refresh_all`'s sweep body is the free fn `bulk_refresh_sweep` (`livrarr-handlers/src/work.rs`): `buffer_unordered(3)`, per-work failure isolation, Bulk surface — pinned by `test_responsiveness_bulk` (bound + exactly-once + counts) and the concurrent-Retry serialization pin in `test_responsiveness_add` (KeyedMutex queues, never rejects). Ops note: `init_tracing` honors `RUST_LOG` when set (replaces the config filter wholesale — the only way to enable non-livrarr targets like reqwest connect tracing); unset = config-derived filter exactly as before. **Measured 2026-07-12: N=3 bulk refresh delivers NO wall-clock win** (2.0 s/work before AND after, 178-work sweep ~6 min) — the sweep is pacing-bound on the shared provider buckets (ST-012's worst case: per-work enrich stretches ~3× under contention, total throughput = bucket throughput). Do NOT reach for more work-level concurrency to speed Refresh All; and do NOT resurrect Hardcover batching for it either — **HC batching (REQ-010) was CANCELLED by the PO 2026-07-12** (documented reason: "Claude was wrong. This is NOT worth it. Gemini and Codex are unanimous in their judgement. This was a goose chase.") because the sweep wall belongs to the Goodreads/Audnexus buckets (161×1.5s / 134×2s on the live library) and the U-B1 cache already zeroes repeat HC fetches; full trail in spec REQ-010 + design §4.2 (baseline doc §7).

71. **Indexer citizenship: origin-keyed buckets, breaker cooldown, live search cache (indexer-citizenship unit, 2026-07-14; contract = `design-indexer-rate-limits.md`, 4-round design review + r6/r7 code reviews).** Every indexer-origin fetch — search, RSS, grab-time torrent/NZB downloads, the magnet-redirect probe — rides `RateBucket::Indexer(<origin>)` where origin = `livrarr_http::normalized_origin(url)` (lowercased `scheme://host[:port]`, default port omitted; ONE authority, in livrarr-http because domain lacks the `url` dep). The bucket key is the CONFIGURED indexer's origin (grab resolves it by display name via list_indexers, falls back to the download-url origin) so same-host indexers share one lane and one cooldown. Indexer buckets are breaker-tracked; a 429 sends TripImmediately with a 30-min cooldown (indexer buckets only — book-provider 429 signals unchanged); callers surface CircuitOpen as a per-indexer warning and skip until cooldown. A RateLimited/CircuitOpen torrent-file fetch FAILS the grab — never falls back to handing the indexer URL to qBittorrent (that would bypass pacing by delegation); other failure kinds keep the URL fallback. Download-client RPC (qBit auth/add, Transmission RPC, SAB POST) is deliberately `RateBucket::None` — classify fetch sites by URL PROVENANCE, never by file location. Search cache: `ReleaseSearchCache` in livrarr-download (key (title, author, indexer_id), 24h TTL) — `cache_only` = zero HTTP with all-miss = successful EMPTY (AllIndexersFailed reserved for live modes); default = per-indexer cached-if-fresh with write-back; `refresh` = always live. The Releases tab mounts cache-only (`modeRef` init "cacheCheck"); the Search button forces live. `HttpFetcher::fetch_no_redirect`'s DEFAULT follows redirects (only HttpFetcherImpl truly suppresses) — test doubles must override both. Pins: `crates/livrarr-download/tests/indexer_citizenship_pins.rs` (7) + `crates/livrarr-http/tests/indexer_breaker_pins.rs` (1) — package-local tests/ dirs are gitignored: new files there need `git add -f`. **#130 RESOLVED — two-level breaker (2026-07-21; contract `docs/design-issue-130-per-indexer-breaker.md` r3, both-family design PASS).** The single origin-keyed breaker blacked out EVERY indexer behind one Prowlarr host on any one indexer's 429 (Prowlarr stores each indexer as `{base}/{id}`, so all normalize to one origin → one shared breaker). Fix: `RateBucket::Indexer(String)` → `Indexer { origin, indexer: Option<String> }` with TWO disjoint breaker levels. TRANSPORT breaker per `origin` (pace-key; `BucketHandle.breaker`; trips on connection/timeout at 5/60s; NEVER sees a 429; shared across the host — restores dead-host protection for the search fan-out). RATE-LIMIT breaker per configured-indexer `id` (`OutboundQueue::rate_limit_breakers`, keyed by the FULL bucket value; a 429 → `TripImmediately`, open = `Retry-After` delta-seconds clamped [10s,6h] else 30-min default; `indexer:None` fallback has none). `do_fetch` owns indexer signal routing: 429 on `indexer:Some` → rate-limit trip + transport SUCCESS (host answered, must not look dead to siblings); 429 on `indexer:None` → transport success only; any completed non-429 response → transport + rate-limit success (closes a half-open). `pace_key()` erases the id for the pace registry; `run_dispatcher` gates a grant on BOTH breakers under a single Phase-1 lock hold (early-drain, no rl-breaker TOCTOU) plus a Phase-3 grant-time re-check (catches a trip during the pacing sleep / a priority reorder). All 6 call sites thread `indexer.id.to_string()`; `indexer:None` only when a grab's indexer name no longer resolves (pace + transport gate still apply). `report_outcome` stays the TRANSPORT reporter (providers unchanged); `report_rate_limit_outcome` is the new per-indexer reporter. Pins: 8 two-level unit tests in `outbound_queue.rs`, Retry-After matrix in `fetcher.rs`, and `indexer_breaker_pins.rs` rewritten to the same-origin-siblings #130 e2e.

70. **ONE qBittorrent state classifier (quality-waves 2a, 2026-07-13).** `classify_qbit_state` (`crates/livrarr-download/src/lib.rs`) maps every qBit state string to BOTH projections at once — `QbitStateClassification { ui_status: QueueStatus, import_safe: bool }` — per the ratified table in `docs/d1-qbit-state-truth-table-2026-07-13.md`. Consumers: the poller import gate (`poll_qbittorrent`) and `queue_service::fetch_qbit_progress` (which serves `ui_status.as_str()` in `QueueProgress.download_status`; the SAB arm still passes SAB's raw status string — no ratified SAB table exists). Never add a local qBit state match anywhere else; extending qBit state handling means editing the classifier AND the table-driven pins (`tests/behavioral/test_qw2_class_a_pins.rs`, `qw2a_*`) together. "Import-safe" = data fully downloaded AND files at their final stable path; `checkingResumeData`, `checkingUP`, and `moving` are deliberately NOT import-safe (the deleted `is_completed_state` triggered import on `checkingResumeData` — the live half-download-import bug 2a killed). The also-deleted `map_qbit_state` was dead plumbing: zero production callers; the queue UI had been receiving the raw state string, unread by the frontend.

72. **Settle-road title trust is cause-aware and single-sourced (identity-fix unit, 2026-07-14; contract = `design-settle-road-matching.md`, 3-round design review + both-family r8 code PASS).** `TitleVerdict::Grey` carries `cause: GreyCause` (OneSidedSubtitle | SubtitleDisagreement | VolumeAsymmetry | NearMain) — computed ONCE in `title_verdict_with_positions`, highest-risk trigger wins, so OneSidedSubtitle means SOLELY that. `identity_matching::title_id_trust(title, a: &IdEvidence, b: &IdEvidence)` is the ONE policy for trusting a text-corroborated identity: per-provider `work_key_contradiction` veto FIRST over RAW evidence (`id_verdict`'s equality-first collapse would mask a mixed agree+contradict payload — that collapse is deliberately UNTOUCHED at the quorum/dedup seats per the ratified "work-key equality wins" ordering; the mixed-case question there is flagged to the matching-conformance cleanup unit), then Same, then Grey{OneSidedSubtitle}. **CORRECTED 2026-07-26 (subtitle-matching C1):** an earlier version of this insight said the grey arm also required `id_verdict ∈ {WorkKeyEqual, EditionBridge}` (the "AC-004 agreeing-ID hatch") — that corroboration requirement is GONE (`identity_matching.rs` `title_id_trust`, the `GreyCause::OneSidedSubtitle` arm returns true outright). A subtitle is edition-level: a work record carries the bare title and an edition record carries title + subtitle, so `EditionBridge` needed ISBN/ASIN equality between two different printings, which by construction it never finds. Equal main titles now stand alone; every caller still applies its own author bar on top, and the same-provider work-key contradiction veto still runs first. Two seats consume it: `verify_gr_payload` (REQ-024 GR-key gate — Same arm keeps author Agree|Abstain, grey arm requires Agree; no self-confirmation possible, the seed carries no gr_key at that gate by precondition) and `flm_match` (REWRITTEN onto the authority, signature `(&Work, &CapturedIdentity)`; the colon-truncating word-set-containment `flm_title` + `canon_author` are DELETED — containment matched "Dune" ⊆ "Dune Messiah" at the last gate before anchors merged, runtime-proven 2026-07-14, violating D3/REQ-001). Consequences: anchor-less seeds auto-merge on Same only (grey parks via the existing `record_pending_anchor` arm — D2's park, anchors held not lost); a contradicting identity merges NOTHING (test_unified_identity_path ac_008's expectation updated per the reviewed ledger); a bare-main work adopts its subtitled GR key iff an independent ID agrees. Both trust declines debug-log the GreyCause. **Fixture lessons that bit this unit:** (a) `StubEnrichmentWorkflow.reset_for_manual_refresh` is counter-only — the real DB reset RECOVERS a terminal NotFound from anchor columns (sqlite_work.rs CASE), so a door test needing that recovery must wire `ResetOnlyEnrichmentWorkflow` (production reset-only type) or settle's REQ-006 terminal guard silently no-ops the whole road (this false-RED hid the try-again fix for a day); (b) 7 UIP fixtures relied on old-containment prefix tolerance ("ACxxx Dune" vs "Dune") — seed titles must match the scripted identity, and each `seed_work` gets its own fresh DB so unique prefixes were never needed. Same unit: the Interactive+NotFound refresh clears anchor dead-ends before the chase gate (work_service REQ-009 block), the identity fan-out responder log carries real values (not presence booleans), and the Unverified tooltip names the real recovery paths. Follow-up unit owns: provider fuzzy pickers (audible.rs shared scorer + google_books.rs), strays V4-V8 from the 2026-07-14 conformance audit, the id_verdict mixed-evidence question at quorum/dedup seats, and review-surface plumbing for uncorroborated grey GR keys.

73. **Three subtitle-variance bugs in one night exposed that `title_id_trust`'s corroboration bar (insight 72) is too strict for a book with mismatched editions — and that OL has TWO more of its own bugs on top (2026-07-15, ad hoc bug-testing session, `subtitle-trust-gaps` feature at `~/Projects/kk-build/build/state/`).** Real case: Walter Isaacson's *The Code Breaker* (OL work `OL24217656W`) hit all three. (1) **OL's edition→ISBN picker had NO filtering at all** (`openlibrary.rs::query_ol_detail`) — took the first edition in the (up to 10) `editions.json` list carrying any ISBN, no language or title check; OL's own edition list for this work leads with the Brazilian Portuguese printing. FIXED: now prefers an edition whose OWN title Same-matches the seed's title (reuses `identity_matching::title_verdict` — the one authority, not a new rule), language as a tie-break among title matches, first-any as the last-resort fallback. **First attempt (language-only, no title check) was insufficient** — live retest picked "Code Breaker -- Young Readers Edition" (an abridged, different product, same language) over the real book; title-matching had to be layered on top of language-matching, not instead of it. 9 tests, real edition data from the live API as fixtures. (2) **The Goodreads key-trust gate (`verify_gr_payload`, insight 72) still rejects a correct GR key** when the seed has no ISBN yet to corroborate against — not because the ISBN conflicts, but because the add door (`resolve_identity_local`, zero-network) creates the work before any provider has been queried, so `captured_from_seed(seed)` has nothing in the isbn/asin/work-key fields for `title_id_trust`'s AC-004 hatch to check against. UNFIXED. (3) **OpenLibrary's search-hit picker rejects its own CORRECT top result** — `provider_client.rs::title_author_search` calls `pick_best_candidate(..., accept_grey=false)`, so ANY subtitle mismatch is fatal, with no ID-corroboration fallback at all (stricter than gate 2, which at least has the AC-004 hatch). Verified live against the real OpenLibrary API: searching the full GR-sourced subtitled title returns the correct work as result #1, but with OL's own bare title — `accept_grey=false` throws it away, and because `resolve()`'s fan-out only logs providers that returned `Success`, this failure is **completely silent**: no error, no warning, no debug line, nothing. UNFIXED. `accept_grey=false` is the SHARED picker for OL/HC/GB/Audible's search-hit sites as of matching-conformance (insight 59, commit `88702f02`) — whether HC/GB/Audible hit the same failure shape for their own bare-vs-subtitled titles is unresearched. A "trust it if the other providers in this quorum round already agree with each other" fix was floated but is unapproved and has a real cost: it changes corroboration from "matches something trusted before any lookup ran" to "matches another equally-unverified guess" — exactly the leniency direction that caused the wrong-book incidents #56/#59 warn about. Two more real bugs found and fixed same session, unrelated to this pattern: a duplicate series row for a partially-linked series (`series_query_service.rs::build_merged_series_list` now also matches an unresolved stub by normalized name, not gr_key equality only — same `identity_key` authority `promote_stub` already used); and import falsely flagging a complete download as partial because the size-completeness check summed only recognized media files against the full torrent size, undercounting real-but-unrecognized extras like a bundled cover.jpg (`import_workflow.rs::enumerate_source_files`/`walk_dir` now track total on-disk size of every file in the same walk). Full research handoff, all citations, and the open design question: `~/Projects/kk-build/build/state/session-log-subtitle-trust-gaps.md`. **Update 2026-07-16/17: the edition-mismatch shape of gate (2) is diagnosed at scale and a fix is BUILT (pending PO validation).** Bazaar forensics: 96/388 works (all English, added Apr 27–Jun 10, all with ISBNs) sat gr-keyless and dead-ended at 3 attempts — the trust gate demands edition-ID equality but GR's title search returns its canonical edition while the work stores the user's edition (live-proven: Sapiens, both GR lookups return the same GR workId 18962767, gate still stripped the key). Fix (Codex-shaped, replacing a rejected workId-corroboration design): `search_resolve_detail` (provider_client.rs) adds an ISBN-first autocomplete tier — the seed's own ISBN returns its edition-exact hit, same junk filter + `pick_best_candidate` bar, detail page then carries the seed's ISBN so the EXISTING AC-004 edition bridge passes with zero trust-rule changes; any abstain/error falls through to the title tier unchanged. Known boundary: decorated edition display titles (e.g. "Sapiens (10 Year Anniversary Edition) /anglais" — trailing text after the parenthetical defeats `parse_title`'s paren peel → Different) still abstain at the ISBN tier; the heavier GR-workId evidence-class design for that residue was reviewed and rejected as unsound (bookId/workId conflation, language-veto bypass) — see `~/Projects/kk-build/build/state/design-gr-work-corroboration-v1-rejected.md`. Rollout plan: after validation, delete the `gr_work` dead-end rows (DB snapshot first) and the hourly convergence job re-chases with the new tier. **Final state (2026-07-17): SHIPPED AND VALIDATED LIVE.** The GR detail-page parser also drifted (Next.js redesign): `parse_detail_html` is now a dispatcher — `parse_detail_next_data` (the `__NEXT_DATA__` Apollo-state blob, primary) → legacy JSON-LD/regex (fallback) → warn-with-shape-detail when both fail. Apollo Book is built field-by-field (`lenient_apollo_book` — a present-but-malformed field drops ALONE and warns; `#[serde(default)]` only covers absent fields), and multiple `getBookByLegacyId` pointers ABSTAIN (never pick by iteration order — Codex-caught wrong-book risk). Fixture: `fixtures/gr-book-23692271.html` (real page; NOTE its `Book.details.isbn13` is genuinely null — GR's canonical Sapiens page carries no ISBN, one of the three independent reasons Sapiens can never self-heal: decorated edition title + ISBN-less canonical page + bare-title grey needing a bridge; manual identity-edit is its path). Live sample after full stack: 3/5 parked works keyed on one convergence pass (Alignment Problem, Make It Stick, Power of Habit); all 92 remaining gr_work dead-ends were then unparked (snapshot `livrarr.db.pre-grunpark-full-*`) for overnight healing; stragglers re-park honestly at 3 attempts. Caveat on the original drift diagnosis: the agent proved LEGACY parsing still works on the browser-fetched fixture — why production parses failed pre-fix was never fully isolated (app-fetched bodies may differ); the blob path empirically fixed live behavior.

74. **Frontend session-fixes trio (2026-07-17, all live-validated).** (a) **EPUB reader + non-linear spine heads:** a spine opening with `<itemref linear="no"/>` (cover) strands epub.js at location 0 — `rendition.next()` from a non-linear section is a NO-OP, and react-reader's built-in arrows call `next()` directly (they bypass any app-level wrapper like `goNext`). Fix in `EpubReader.tsx`: on `book.ready` with no saved position, `setLocation` to the FIRST LINEAR spine item (`spine.items.find(linear !== "no")`); saved-progress CFIs win either way because both paths route through `setLocation(current === 0 ? … : current)`. `goNext`'s location-0 branch also targets the first linear item (jumping past a non-linear head IS the advance — don't chain `next()`). (b) **Toast dismissal contract (`NotificationBell.tsx`):** a sonner toast's X only closes the toast — the server row stays unread and re-toasts on every bell remount/page load; persistent toasts must carry a stable `id` (re-issue REPLACES instead of stacking duplicates) and an `onDismiss` that calls the real dismiss API. The server-side unread feed correctly excludes dismissed rows (`sqlite_notification.rs:85`) — the resurrection class is purely frontend. (c) **pathNotFound is transiently true during the seedbox→NAS transfer window:** qBit reports complete while the file hasn't reached the local mount; the import later succeeds through the mapping (Worldly Philosophers, grab 51: warned 00:23, imported clean 01:22). Possible refinement (not built): delay/retract the alert across the transfer window. Also learned en route: series "Monitor" buttons must forward the row's `grKey` (the author-series listing serves REAL keys even for rows with no DB id; only genuine stub DB rows are masked to "") — fixed at both doors (`AuthorDetailPage.tsx` SeriesRow + `SeriesPage.tsx` AuthorSeriesExpanderRow), root-caused by the fix agent against my wrong masking hypothesis.

75. **`dev-restart.sh` green ≠ CI green for the frontend: the dev script never typechecks (2026-07-18, broke the first alpha6 release build).** The script runs `npx vite build` directly — vite does NOT run TypeScript. CI's Docker build runs `pnpm build` = `tsc --noEmit && vite build`. A strict-mode-only error (TS2722: an indexed `Record` lookup must be bound to a local before calling — the ternary guard does not narrow a repeated index access) shipped through dev-restart + live validation and killed both release build legs at the frontend stage. Before committing any frontend change (and always before tagging): `cd frontend && npx tsc --noEmit`. Same session, release-ops lessons: (a) a background `gh run watch --exit-status` wrapper whose LAST command is an `echo` reports exit 0 regardless — read the run's real conclusion via `gh run view --json conclusion`, never the wrapper's exit; (b) `git cliff --tag vX` while an OLD tag of the same name still exists splits the release into duplicate headers — regenerate the changelog only when the tag state is final (or after deleting the stale tag); (c) a failed release build that never pushed artifacts is recoverable by delete-tag → fix → re-tag (release-check's own remedy), but once an image is published and pulled, the tag NEVER moves — patch forward. Pending-guess trust fixes shipped in the same alpha6 window (settled-slot rule): the resolver never parks, the list endpoint never serves, and the affirm door 409s a guess for an anchor slot that is already settled (confirmed ledger row or populated works column) — `confirm_anchor_in_tx` overwrites `works.*` unconditionally, so an offered competing guess was one click from silently replacing a user-confirmed identity (found live: work 34's canonical-OL guess vs its user-confirmed OL key). Guess ids in the banner now link out (GR `/book/show/`, OL `/works/`, isbnsearch, Amazon `/dp/`); Hardcover guesses are hidden entirely — hc_key is an internal numeric id with no public page to verify against. Pin: `test_id_completeness_settled_slot_guesses_are_hidden_and_unaffirmable`.

66. **Interactive add is fast-path + background completion (responsiveness Lane A, 2026-07-11).** `POST /works` no longer blocks on providers: the handler derives identity LOCALLY (`WorkService::resolve_identity_local` — sync, zero network: work anchor → Confirmed, bridge/none → Pending), calls `add_fast` (all of old `add` through phase-1 cover + badge persist, PLUS a verdict-gated bridge dedup: `find_works_by_bridge(user, isbn, asin)` hits dedup iff `title_verdict ∈ {Same, Grey}` AND `author_verdict ≠ Disagree` — collision shapes create separately per the bridge-anchor policy), then spawns `complete_add` (the identity fan-out + enrichment + cover gates, wrapped in the in-memory `enriching` registry guard; +5s anchor top-up refresh CHAINED after completion, never parallel). `ensure_identity_and_enrichment` remains ONE gated road — Pending/Conflict/NeedsReview blocks enrichment for every caller including complete_add (an implementing agent's bypass variant was reverted; the behavioral test pins the gate). `is_enriching` is process-local by design (false after restart; convergence owns durable recovery); `WorkDetailResponse.enriching` + `AddWorkResponse.created` are the API surface; the detail page pill/poll (1.5s→5s, 60s cap) + skeletons consume it. Existing sync callers of `add` are UNCHANGED (`add` = `add_fast` + awaited `complete_add`). Measured: add POST 19.8s/3.1s/2.1s → 3.0s/7ms/513ms (ceiling = the 3s phase-1 cover budget). Grids serve the existing 300px thumbs via `BookCover variant` (default thumb, detail hero full) — 3.65× lighter first view. _U-C2 (2026-07-12):_ all four image routes honor a `?v=` token (`CoverQuery` in `livrarr-handlers/src/mediacover.rs`) — versioned requests return `Cache-Control: public, max-age=31536000, immutable` (repeat views = zero image requests; the UI already sends `?v=<cover_mtime>`, and a cover change bumps the mtime → new URL), unversioned keep `no-cache`+ETag, missing covers stay `no-store`. Tests: `tests/behavioral/test_responsiveness_add.rs` (8). Baseline/after: `docs/speed-baseline-2026-07-11-responsiveness.md`.

76. **`MergeOutput.work_update` is a last-known-good ECHO — presence is NOT a change signal — and `ApplyMergeOutcome::NoChange`/`Deferred` have ZERO producers** (`SqliteDb::apply_enrichment_merge` returns only `Applied`/`Superseded`). Found at work-history Stage 5 (2026-07-19): the design assumed presence-derived `changed` meant "fields changed"; in fact it was true on every completed merge with any eligible provider (`merge_engine.rs` emits the echo unless providers were attempted AND all excluded). Truthful change detection is `content_changed` (`livrarr-enrichment/src/lib.rs`): a pre/post work-row diff excluding the bookkeeping the apply always stamps (`enrichment_status`, `enrichment_source`, `enriched_at`), computed at both `enrich_work` completion arms; `Work` derives `PartialEq` for it. Consequence: `EnrichmentResult.changed` is truthful, so materialize's change gate (tag rewrite) now skips genuinely no-op passes — before this it re-tagged on every completed pass, and the work-history `enriched` event would have said `changed:true` forever.

77. **Work history is complete and single-authority (work-history feature, 2026-07-20, `aa7f6985`).** 14 event kinds, every lifecycle moment writes exactly one event at its moment of truth. **Payloads:** built ONLY via the typed constructors in `livrarr-domain/src/history_events.rs` — never ad-hoc `json!` at a writer site; work-attached payloads carry `work_title` (+`work_author` where sensible) so rows stay identifiable after `history.work_id` nulls on work delete; grab-family `title` means the RELEASE title, never the work's. **Write paths:** `record_history` (livrarr-db; infallible observer — warns, never propagates, REQ-009) for in-crate writers; the domain `HistoryService::record` trait method for the four compile-walled handler doors (identity review-apply, pending-anchor affirm, manual-import confirm loop at import_single_item's sole caller, root-folder adopt). **Moments:** birth = one `added` at `finish_created_work_fast` entry (the sole `created:true` constructor; the source door label is stamped inside the six `seed_*` constructors via `WorkCandidate.add_source` — a caller cannot mislabel); enrichment = exactly one of `enriched{changed,status,tags_written}`/`enrichmentFailed`/nothing per `run_unified_enrichment` invocation, nothing ⟺ `merge_deferred` ∨ `!attempted` (`EnrichmentResult.attempted` is authored by `enrich_work`'s own control flow — the cached-reuse arm completes with an EMPTY outcomes map, so outcome-emptiness is NOT a no-attempt signal; `changed` = the `content_changed` row diff, insight 76); tag events = one per work per pass inside `retag_library_items` (covers all three callers) + per-item `tagWritten` on convergence-recovery success + NO separate event for materialize's in-enrichment write (it rides `enriched.tags_written` — one moment, one event); deletes = `fileDeleted` per user file door, `workDeleted` composite with `work_id` NULL written AFTER successful deletion (D-DELETE-ORDER — before-delete risks false events, after-delete can't carry the FK), Readarr undo emits per-file + per-orphan events `undo:true`; `merge_works` repoints loser history to the survivor inside the merge tx (the old "cascades away" comment was WRONG — ON DELETE SET NULL orphaned them) + one `worksMerged` on the survivor; `identityResolved` fires on the four USER identity doors only (review dismiss and system-side anchor writes deliberately excluded). **SAB failure arm:** `downloadFailed` writes ONLY after `update_grab_status` returns Ok — on a failed update the grab stays active and the poller re-enters the arm next tick, so an early write duplicates (code-review r1). **Robust reads:** an unrecognized stored `event_type` is skipped-with-warn in both list methods (rollback safety); the paginated COUNT stays raw — documented accepted drift (D-COUNT), do not "fix" it. **Backfill** (`crates/livrarr-server/src/jobs/history_backfill.rs`, 4th startup pass): one-time (marker `history_backfill_generation` in `_livrarr_meta`, written via INSERT..ON CONFLICT — the key is unseeded, a bare UPDATE silently no-ops), synthesizes added/last-enriched/grabbed/imported + grab-failure events from persisted timestamps only, every row `backfilled:true` with the FACT date; per-fact dedup: grabbed by payload `guid`; failures TWO-TIER (a guid-carrying row — backfill-written — covers exactly its grab; a guid-less row — live writers — covers by (work_id, release title)); imported covered by a same-work Imported/ImportFailed row dated within [imported_at−5s, +1h] inclusive; grabbed/failure synthesis ONLY for Imported/Failed/ImportFailed grabs — Sent/Confirmed/Importing/Removed synthesize NOTHING at all; inserts are fallible per-row, any failure skips the marker and the next boot's rerun is additive; `DELETE FROM history` is P0-forbidden everywhere including idempotency. First live run: 6 → 420 rows, every count reconciled exactly (143 added / 142 enriched / 129+3 imported / 3 grabbed all pre-covered). **Frontend:** the `EventType` TS union drives three compiler-forced Record maps in HistoryPage; the runtime `??` fallbacks ARE the version-skew rendering — load-bearing, never remove; deleted works render `data.work_title` before "—".
78. **Anchor uniqueness is per-user over ALL anchor types — live since migration 044; there is NO cross-user contention (verified empirically 2026-07-24).** The history: 041 created a GLOBAL `uniq_user_confirmed_ol_anchor (anchor_type, anchor_value)`; **042 dropped it** (`042_fix_anchor_user_scope.sql`); 044 added+backfilled `user_id` and recreated the SAME NAME as `(user_id, anchor_type, anchor_value) WHERE confidence='confirmed'` — because 042 had dropped the old one, 044's `IF NOT EXISTS` was NOT a no-op. A false contrary claim ("041's global is live; 044 no-opped", codex r2 P2-1 "VERIFIED") survived identity-edit design rounds r2→r4 and three model families for a day — it was inferred from reading 041+044 without enumerating the files between them, and was caught only by two independent test suites' contradictory red-runs. Lessons (RCA in `~/Projects/kk-build/build/state/session-log-identity-edit.md` 10:52Z): a "current schema state" claim requires enumerating the FULL migration range, never endpoints; a review finding that REVERSES a prior factual claim needs a decisive executable probe (a scratch-DB migration replay costs ~1 min), never role-authority deference. identity-edit's migration 076 (unshipped) narrows the index to work-keys-only per-user (same-user shared bridges become legal — the ONLY semantic delta).

79. **The process-global outbound queue can wedge: a dispatcher dies with the runtime that spawned it, and every caller already parked in `acquire` waits forever (identity-edit contest, 2026-07-24/25).** `DispatcherGuard` clears `dispatcher_running` when a dispatcher dies, but that only helps the NEXT arrival — items already in the heap starve if every remaining caller is already blocked on its turn. This is exactly what happens when several `#[tokio::test]` runtimes share the one process-global queue (insight 30/58's global-state pain). **Strength of evidence: THREE of the four independent contest implementations, with no knowledge of each other, added the same fix to `crates/livrarr-http/src/outbound_queue.rs`** — a periodic re-check in the `acquire` wait that respawns the dispatcher when `!dispatcher_running && !heap.is_empty()` (codex 100ms, opus 250ms, fable; CC alone did not, and CC's gated suite is the only one that hangs under default test parallelism — killed at 7min where the others finish in ~37s). Isolated by experiment: applying codex's diff to the CC tree turned a 7-minute hang into a 36.82s completion, single variable, under heavier load than the control. In production one runtime owns the process, so the watchdog never fires — the wedge is a test-environment manifestation of a real latent hole in shared infra. **Do not read "only CC hangs" as "CC's bug"** — the hole is in `livrarr-http`; CC's provider-call pattern is merely the one that trips it. Whichever entry merges, the queue fix should ride along. **RESOLVED 2026-07-25: fable merged, and its tree carries the watchdog (+47/-12), so it did ride along. Whether to KEEP it is still an open PO call — it is a `livrarr-http` change, not an identity-edit one.**

80. **Identity edits are protected by a generation stamp, and the guard is only as good as the writer that honours it (identity-edit merged 2026-07-25, `a7f03540`).** `works.identity_generation` (migration `076_anchor_uniqueness_identity_generation.sql:12`) is the coordination primitive for every identity writer. The protocol is: read `(Work, identity_generation)` in ONE transaction **before** provider I/O, then submit exactly one claimed `complete_anchors(work_id, expected_generation, completion)` **after** it; a lost claim returns `IdentityCompletionOutcome::Superseded` and writes **nothing**. The canonical implementation is `crates/livrarr-identity/src/async_resolver.rs:132` (pre-await read) and `:259` (claimed completion); all six resolver roads funnel through it. `tests/behavioral/test_identity_edit_race_repro.rs` pins the refresh road — real refresh, real HTTP clear door, resolver parked via `StubProviderClient::with_delay`, proceeding on `call_count() > 0` and never a timer.

    **The trap, and it is the whole lesson:** the CAS is easy to have and easy to render useless. Two ways, both live in the tree today. (a) **Never claiming** — the post-await write calls the legacy unguarded repository method (`merge_missing_anchors`, `set_identity_confirmed`). That is what the losing contest entries did, and the race repro catches it. (b) **Claiming a generation observed too late** — `complete_add`'s delayed-NotFound arm (`crates/livrarr-metadata/src/work_service.rs:1192-1197`) reads `(work, generation)` *after* enrichment already returned, so it faithfully claims a generation its decision never saw and stamps a stale conclusion over the user's edit. Form (b) passes every "is there a CAS?" review and is invisible to the race repro. **The question to ask a writer is never "does it claim?" but "was the claimed generation observed at the moment the decision was made?"** Convergence's Pending→NeedsReview arm (`crates/livrarr-metadata/src/convergence_service.rs:50` read, `:92-96` unconditional write) and add/adopt preflight (`crates/livrarr-metadata/src/work_service.rs:288-310`, three separate transactions, no generation at all) are the other two writers still outside the protocol. All tracked in `docs/design-identity-edit-fixes.md`.

    Also load-bearing: `work_identity_anchors` carries its own `user_id` (same migration, `:6`). Ownership questions must be answered by querying that table directly — the merged backfill (`crates/livrarr-db/src/sqlite_work_identity.rs:2132-2163`) instead builds an owner map from works that have a non-NULL legacy column, so a work owning a confirmed ledger row with all five columns NULL is invisible, a second owner gets inserted, and the `uniq_user_confirmed_work_anchor` index **fails startup**. `race/cc`'s `work_identity_ledger_backfill.rs:222-224` shows the direct-ledger form.
